
// Converted from TypeScript interfaces to JavaScript objects

// This file contains type definitions for the PhoneDialer component
// Since we're converting to JavaScript, these types are now just documentation
// and aren't enforced by the type system

/*
export interface PhoneDialerProps {
  contactNumber?: string;
  contactName?: string;
  onCallComplete?: (duration: number, notes: string) => void;
  isImmediate?: boolean;
  isScheduled?: boolean;
  scheduledTime?: string;
  scheduledDate?: string;
  participants?: Array<{name: string, email: string}>;
}
*/
